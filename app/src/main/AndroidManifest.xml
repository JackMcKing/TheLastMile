<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wangchenguang.thelastmile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Start"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity -->
        <!-- android:name=".Supermarket" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity
            android:name=".Courier"
            android:label="@string/title_activity_courier"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".User"
            android:label="@string/title_activity_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainMenu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UserLogin"
            android:label="@string/title_activity_user_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SupermarketLogin"
            android:label="@string/title_activity_supermarket_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CourierLogin"
            android:label="@string/title_activity_courier_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Logup"
            android:label="@string/title_activity_logup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Supermarket"
            android:label="@string/title_activity_supermarket_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <!-- <activity -->
        <!-- android:name=".Start" -->
        <!-- android:label="@string/title_activity_start" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"></activity> -->
        <activity
            android:name=".SendMessage"
            android:label="@string/title_activity_send_message"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Recieve"
            android:label="@string/title_activity_recieve"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
